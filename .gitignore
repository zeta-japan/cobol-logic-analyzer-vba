# Excel workbooks - excluded until Phase 5 ships a finalized CobolAnalyzer.xlsm.
# When ready to publish, use `git add -f CobolAnalyzer.xlsm`.
*.xlsm
*.xlsx
*.xlsm.bak

# Excel temp / lock files
~$*.xlsm
~$*.xlsx

# Office cache
.~lock.*

# Engine output generated by Main.AnalyzeAndBuild (debug artifact)
*.vba.logic.json

# Backup
*.swp
*.bak
*.orig

# OS
Thumbs.db
.DS_Store

# IDE
.vscode/
.idea/

# Local snapshot archives (kept for personal record, not for the repo)
Commit-*/

# Ad-hoc work files dropped into samples for analysis - NEVER commit.
# Keep them outside the repo if possible.
samples/input/*.txt
